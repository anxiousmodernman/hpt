#!/usr/bin/env ion

let images = $(doctl compute image list | awk '{if($2 ~ /hpt-test/) print $1}')

for image_id in $images
    echo "deleting image id: $image_id"
    doctl compute image delete $image_id --force
end


